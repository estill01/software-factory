name: Software Factory v2 controller/provider landing (paused)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Landing is paused while the immutable corrected slice is uploaded."
